# MongoDB Credentials
MONGO_USER=mongo_user
MONGO_PASS=mongo_pass

# MongoDB Connection String (Used by backend service in docker-compose)
# Note: The backend application itself (database.py) uses MONGO_DETAILS,
# which is constructed within the docker-compose.yml file.
# This MONGO_URI is here for completeness or if other services need it.
MONGO_URI=mongodb://mongo_user:mongo_pass@mongo:27017/spaced_repetition?authSource=admin

# Other backend environment variables can be added here if needed
# Example: SECRET_KEY=your_secret_key
# JWT_SECRET_KEY=your_jwt_secret
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30
